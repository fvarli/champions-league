# Production environment for the Vue build (native VPS).
# Copy to frontend/.env on the server before running `npm run build`.
#
# The API shares the domain, so this is just the site origin WITHOUT the /api
# suffix (the HTTP client already prefixes requests with /api).
VITE_API_URL=https://champions.ferzendervarli.com
