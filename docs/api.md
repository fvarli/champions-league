# API

## Conventions

- Base URL (local): `http://localhost:8080`
- All responses are JSON.
- Request and response bodies use `application/json`.
- Endpoints will be versioned under `/api` as they are introduced.

## Endpoints

No domain endpoints exist yet (Phase 1).

| Method | Path  | Description                  |
| ------ | ----- | ---------------------------- |
| GET    | `/up` | Application health check     |

This document is updated alongside each new endpoint.
